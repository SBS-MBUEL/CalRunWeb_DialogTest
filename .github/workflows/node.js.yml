
name: Node.js CI

on:
  push:
    branches: '**'
  pull_request:
    branches:  '**' 

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x, 15.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install yarn
    - run: yarn
    - run: yarn test
    - name: Jest Code Coverage Report
      # You may pin to the exact commit or the version. (coverage)
      # uses: ziishaned/jest-reporter-action@872c1051350ecaf625ba2490d68e7cb63854b44c
      # uses: ziishaned/jest-reporter-action@0.1.1
      # with:
      #   # Github token
      #   github-token: 
      #   # Specify a command to run the tests
      #   test-command: test
